# Copy to .dev.vars for local `wrangler dev` (gitignored by default).
# For production, use `npx wrangler secret put <NAME>` for each secret below.

JWT_SECRET=change-me-to-a-long-random-string
R2_SECRET_ACCESS_KEY=
RAZORPAY_KEY_SECRET=
